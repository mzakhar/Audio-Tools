apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synth-discovery-leads
  namespace: synth
  labels:
    app.kubernetes.io/name: synth-discovery
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
