apiVersion: v1
kind: Service
metadata:
  name: synth-discovery
  namespace: synth
  labels:
    app.kubernetes.io/name: synth-discovery
spec:
  selector:
    app.kubernetes.io/name: synth-discovery
  ports:
    - name: http
      port: 80
      targetPort: http
