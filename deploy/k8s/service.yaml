apiVersion: v1
kind: Service
metadata:
  name: synth
  namespace: synth
  labels:
    app.kubernetes.io/name: synth
spec:
  selector:
    app.kubernetes.io/name: synth
  ports:
    - name: http
      port: 80
      targetPort: http
